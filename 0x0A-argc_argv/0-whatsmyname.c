#include <stdio.h>

/**
 * main - prints the number
 * of argumenets passed
 * @argc: int count of arguments
 * @argv: vector array of arguments
 * Return: 0 success
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
