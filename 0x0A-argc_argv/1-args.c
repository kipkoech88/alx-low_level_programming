#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of command line argumentd
 * @argc: argument count
 * @argv: argument array vector
 * Return: Always (0);
 */

int main(int argc, char __attribute__ ((unused)) *argv[])
{
	printf("%d\n", argc);

	return (0);
}
