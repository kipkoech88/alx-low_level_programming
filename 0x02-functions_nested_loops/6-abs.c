#include "main.h"

/**
 * _abs - checks for absolute value of a number
 *@n: number to get abs value
 * Return: 0 success
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
